[OOM]
# Systemd-oomd memory pressure monitoring and OOM handling

# How long to observe memory pressure before taking action
# 10 seconds = react quickly to memory pressure
DefaultMemoryPressureDurationSec=10s
